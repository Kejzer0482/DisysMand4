syntax = "proto3";

option go_package = "RequestCP/grpc/proto";

message RequestMessage {
    string username = 1;
    string message = 2;
    int64 timestamp = 3;
}

message Empty {}

service RequestService {
    rpc Request(stream RequestMessage) returns (stream RequestMessage);
}